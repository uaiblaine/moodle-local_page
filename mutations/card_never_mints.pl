# card_never_mints: have the listing card mint the code it shows instead of reading it. The listing is
# a GET, and a GET that writes turns every visit to the pages screen into a write to core's table.
s{\\local_page\\local\\links::existing_share\(\(int\) \$this->id\)}{\\local_page\\local\\links::share(\$row)}s;
