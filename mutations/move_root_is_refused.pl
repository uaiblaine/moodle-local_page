# move_root_is_refused: lifecycle::category_moved() no longer refuses a move to the root, and resolves
# the root's category context instead, which does not exist.
s{\n        if \(\$newparentid <= 0\) \{\n            foreach \(\$rows as \$row\) \{\n(?:[^\n]*\n){4}            return;\n        \}\n}{\n};
